{
  "name": "foxdock-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "sync:version": "node scripts/sync-version.mjs",
    "check:version": "node scripts/check-version.mjs",
    "dev": "vite",
    "prebuild": "pnpm run sync:version",
    "build": "pnpm run check:version && vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "pretauri": "pnpm run sync:version",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-updater": "^2",
    "vue": "^3.5.13",
    "vue-i18n": "^11.3.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
